import { Typography, Colors, Constants, Assets } from 'react-native-ui-lib';

Colors.loadColors({
  purple: '#452D97',
  iosBlue: '#007AFF',
});

Typography.loadTypographies({
  strong: { fontWeight: '700' }
});

// Assets.loadAssetsGroup('splash', {
//   splashScreen: require('../assets/timeline.png'),
// });